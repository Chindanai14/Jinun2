package com.Aaeli.jinun;

import net.fabricmc.api.ModInitializer;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Jinun implements ModInitializer {
    public static final String NAME   = "Jinun";
    public static final Logger LOGGER = LoggerFactory.getLogger("Jinun");

    @Override
    public void onInitialize() {
        // common / server-side init here (ปล่อยว่างได้)
    }
}